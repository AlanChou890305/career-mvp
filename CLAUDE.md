# career-mvp — 共同前提

第三組 B 組（Alan、Berry、Sunny）的職涯探索工具專案。這份文件是每個人的 Claude Code session 共用的前提，讓三支分支基於同一組理解發展。

完整內容在 `docs/`，這裡是濃縮版。

---

## 我們在解什麼

職涯迷惘不是資訊不足的問題。台灣市場上「該做什麼工作」的資訊已經過剩——免費測驗、社群討論、內容平台、AI 工具都在給答案，但迷惘的人依然迷惘。

問題在於**無法消化自己的經歷，也無法驗證答案對不對**。

## 價值主張

> 我們幫助對職涯方向感到迷惘的人，在整理過往經歷卻看不出方向線索的時候，透過 AI 經歷歸納與投遞數據追蹤，解決「無法消化自己的經歷，也無法驗證方向對不對」的問題。

## 兩個核心利基點

| 代號 | 利基點 | 為什麼市場是空的 |
|---|---|---|
| **N16** | 經歷歸納協助度 | 多數工具只收集資料或只給結論，中間「幫你歸納」那一步是空的 |
| **N19** | 可驗證性 | 18 個競品的可驗證性分數全部偏低——給完建議就結束，沒有人追蹤準不準 |

兩者是同一段旅程的前後兩步：先把經歷整理成假設，再驗證假設對不對。N16 是入口，N19 提供難以複製的長期價值。

**護城河是記憶與累積，不是更聰明的建議。** 任何「更好的 AI 回答」都在 ChatGPT 的射程內，沒有防禦性。

## 這次 MVP 在驗證什麼

這是**驗證實驗，不是產品原型**。不做前端、不做完整閉環、不做登入或資料庫。

驗證兩個核心價值：

1. **進階問題** — AI 依使用者背景與 JD 追問的問題，命不命中面試官真正會問的
2. **行動推薦** — AI 給的準備建議，有沒有人真的照做並派上用場

驗證方法是雙軌的：進階問題用公開面試心得語料做盲測（目標 30 筆，三人各 10 筆），行動推薦用真實面試做質性訪談。詳見 `docs/validation-plan.md`。

## 幾個容易走偏的地方

- **不用「面試上了沒」當判準。** 那是一個 bit 的雜訊，被薪資、內定、面試官偏好污染。標準答案是「面試官實際問了什麼」。
- **AI 生成的 dummy data 不能驗證 Persona。** 從 Persona 描述生成資料、再歸納回 Persona，是循環論證。dummy data 只用來測系統邊界（資料過短、跨產業、內容矛盾、超長輸入）。
- **適配度判斷不是驗證指標。** 可以順手記錄當觀察值。
- **不要把功能做多。** 推演結論明確反對「更多功能」這條路，護城河在累積不在廣度。

## 分支規則

`main` 只放共用文件與素材，不放應用程式碼，且已開啟保護，不能直接推。

- 做自己的 MVP → `mvp/alan`、`mvp/berry`、`mvp/sunny`，待在自己的分支
- 改共用文件（`README.md`、`CLAUDE.md`、`docs/`）→ 開 `docs/主題` 分支，發 PR
- 分支之間不互相合併，只從 `main` 單向同步

Berry 和 Sunny 是第一次用 git。協助 git 操作時：先確認人在哪個分支，動到 `main` 之前先問，不要用 `--force`、`reset --hard`、`clean -fd` 這類會丟掉工作的指令。

## 寫文件的語氣

寫成事實與決定的紀錄：「我們決定做 X，因為 Y」。不用祈使句訓話、不寫「你必須」「切記」、不加警語圖示。列表就是列表。

## 用詞

以 `docs/glossary.md` 為準。常見的幾個：痛點原型用 A–I，利基點用 N + 象限編號，Persona 用英文名，旅程階段用 ①–⑥。
