1. 你在第一份工作從零建立公司的系統與環境——當時是誰決定要自建、而不是用現成的託管服務或框架？技術選型的決策權在你手上多少？
2. 你架 LAMP 伺服器時，PHP-FPM 的 process manager 是用 dynamic 還是 static，pm.max_children 你怎麼算出那個數字的？
3. 你提到有 Redis 快取經驗——那個系統裡快取的是哪一類資料，你怎麼處理快取失效？是設 TTL、主動清除，還是有其他機制？當時遇過快取雪崩或髒資料嗎？
4. Laravel 的 Eloquent 在關聯查詢上很容易踩 N+1，你怎麼發現這個問題、又怎麼解掉？eager loading 之外你還用過什麼手段？
5. MySQL 的複合索引有最左前綴原則，如果一張表上有 `(a, b, c)` 的索引，`WHERE b = ? AND c = ?` 會不會用到？你在實務上怎麼判斷該不該加索引？
6. Laravel 的 Queue 你有用過嗎？driver 選什麼、失敗的 job 怎麼重試、怎麼避免同一個 job 被重複執行？
7. 給你一個 log 檔，裡面有上億筆記錄，要找出出現次數最多的前 10 個 IP，記憶體只有 1GB。你會怎麼做？
8. 你是非本科自己修完資工必修的——遇到團隊裡本科出身、技術判斷跟你不一樣的同事，你怎麼處理？有沒有實際發生過的例子？
